# The image copies only its manifests and source package. Keep local
# environments, credentials, caches and fixtures out of every build context.
*
!pyproject.toml
!constraints.txt
!README.md
!src/
!src/**
