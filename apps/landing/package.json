{
  "name": "@pruevia/landing",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nuxt dev --host 127.0.0.1 --port 3000",
    "build": "nuxt build",
    "generate": "nuxt generate && node scripts/render_headers.mjs",
    "preview": "nuxt preview",
    "typecheck": "nuxt typecheck",
    "test": "node --test tests/*.test.mjs",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "nuxt": "4.5.2"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "typescript": "~5.9.2",
    "vue-tsc": "^3.0.0"
  }
}
